<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="java.hospital.adminhosptial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminHosptial">
        <activity
            android:name=".MostrarListaDos"
            android:exported="false" />
        <activity
            android:name=".ListaPacientesUno"
            android:exported="false" />
        <activity android:name=".ModInfo" />
        <activity android:name=".MostrarLista" />
        <activity android:name=".InfoAdmin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>